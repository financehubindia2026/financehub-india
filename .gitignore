# Dependencies
node_modules/

# Environment variables — NEVER commit real API keys
.env
.env.local
.env.*.local

# Vercel local project link/config
.vercel

# OS / editor noise
.DS_Store
*.log
npm-debug.log*

# Build artifacts (none expected for this static project, but just in case)
dist/
.cache/

# Regenerated by scripts/publish.js — build cache & manifest, not source of truth
generated/
